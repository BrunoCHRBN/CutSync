{
  "installCommand": "npm install --legacy-peer-deps --no-audit --no-fund --no-progress",
  "buildCommand": "npm run build:web",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}