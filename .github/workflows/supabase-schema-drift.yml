name: Supabase Schema Drift

on:
  pull_request:
    paths:
      - 'supabase/**'
      - 'packages/database/src/supabase.generated.ts'
      - 'scripts/*supabase*.sh'
      - '.github/workflows/supabase-schema-drift.yml'
  push:
    branches: [main, master]
    paths:
      - 'supabase/**'
      - 'packages/database/src/supabase.generated.ts'
      - 'scripts/*supabase*.sh'
      - '.github/workflows/supabase-schema-drift.yml'
  schedule:
    - cron: '17 8 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  schema-drift:
    name: Comparar schema remoto e tipos
    runs-on: ubuntu-latest
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      # Project refs are public identifiers. Keep the drift target explicit so
      # this workflow can never compare or generate types from production.
      SUPABASE_PROJECT_ID: sphbbqdgcreowxzjgibj
    steps:
      - name: Baixar código
        uses: actions/checkout@v4

      - name: Validar secrets obrigatórios
        shell: bash
        run: |
          if [[ -z "$SUPABASE_ACCESS_TOKEN" ]]; then
            echo "Configure SUPABASE_ACCESS_TOKEN nos secrets do repositório." >&2
            exit 1
          fi

      - name: Instalar Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Verificar divergência do schema
        run: bash scripts/check-supabase-schema.sh
